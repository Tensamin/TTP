[package]
name = "wasm"
version = "0.1.0"
edition = "2024"

[dependencies]
epsilon-core = { path = "../core" }
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["WebTransport"] }
async-trait = "0.1"
